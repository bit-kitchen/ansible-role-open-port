#!/usr/bin/env ansible-playbook
---
- hosts: localhost
  gather_facts: yes
  become: yes
  roles:
    - name: ../..

      #open_port: 80
      #open_protocol: tcp

      # GCP firewall variables (optional)
      #gcp_project: ""
      #gcp_service_account_email: ""
      #gcp_rule_name: ""
      #gcp_rule_description: ""
